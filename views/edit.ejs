<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Song</title>
    <link rel="stylesheet" href="/styles2.css"> <!-- Link to your existing styles -->
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #2E1A47; /* Dark Purple */
            color: #FFFFFF;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px;
            text-align: center; /* Center text for the container */
        }

        header {
            background-color: #5D3FD3; /* Medium Purple */
            padding: 15px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        header h1 {
            margin: 0;
            font-size: 24px;
            color: white;
        }

        header nav ul {
            list-style: none;
            margin: 0;
            padding: 0;
            display: flex;
            gap: 20px;
        }

        header nav a {
            text-decoration: none;
            color: white;
        }

        .form-card {
            background-color: #4A306D; /* Darker Purple for the card */
            padding: 30px; /* Padding inside the card */
            border-radius: 12px; /* Rounded corners */
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5); /* Shadow for depth */
            margin: 20px auto; /* Center the card */
            max-width: 600px; /* Max width for the card */
            text-align: left; /* Left align text inside the form card */
        }

        .form-card h2 {
            margin-bottom: 20px; /* Space below the heading */
            color: #FFFFFF; /* White text for heading */
        }

        .form-row {
            display: flex; /* Use flexbox for label and input alignment */
            flex-direction: column; /* Align items vertically */
            margin-bottom: 15px; /* Space between rows */
        }

        .form-card label {
            margin-bottom: 5px; /* Space below labels */
            color: #FFFFFF; /* White color for labels */
            font-weight: bold; /* Bold text for labels */
        }

        .form-card input[type="text"],
        .form-card textarea,
        .form-card input[type="file"] {
            width: 100%; /* Full width input */
            padding: 10px; /* Padding inside inputs */
            border: 1px solid #888; /* Light gray border */
            border-radius: 4px; /* Rounded corners */
            box-sizing: border-box; /* Include padding in width */
            background: #555; /* Darker background for inputs */
            color: white; /* White text color */
        }

        .form-card button {
            background-color: #5D3FD3; /* Button color */
            color: white; /* Text color */
            padding: 12px; /* Padding for button */
            border: none; /* Remove border */
            border-radius: 5px; /* Rounded corners for button */
            cursor: pointer; /* Pointer cursor */
            font-size: 18px; /* Larger text for button */
            width: 100%; /* Full width button */
            transition: background-color 0.3s, transform 0.2s; /* Smooth transitions */
        }

        .form-card button:hover {
            background-color: #743DA2; /* Lighter Purple on hover */
            transform: translateY(-2px); /* Slight lift effect on hover */
        }

        .form-card .back-link {
            display: block; /* Block element for link */
            margin-top: 15px; /* Space above link */
            color: #9D79E9; /* Lighter purple for link */
            text-decoration: none; /* Remove underline */
            font-weight: bold; /* Bold link text */
            text-align: center; /* Center link */
        }

        .form-card .back-link:hover {
            text-decoration: underline; /* Underline on hover */
        }

        footer {
            margin-top: 40px;
            padding: 20px;
            background-color: #5D3FD3; /* Medium Purple */
            color: white;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>IrohBeats</h1>
            <nav>
                <ul>
                    <li><a href="/home">Home</a></li>
                    <li><a href="#">Search</a></li>
                    <li><a href="#">Library</a></li>
                </ul>
            </nav>
        </header>
        <main>
            <div class="form-card">
                <h2>Edit Song</h2>
                <form action="/edit/<%= song.Id %>" method="POST" enctype="multipart/form-data">
                    <div class="form-row">
                        <label for="title">Song Title</label>
                        <input type="text" id="title" name="title" value="<%= song.title %>" placeholder="Enter song title" required>
                    </div>

                    <div class="form-row">
                        <label for="artist">Artist Name</label>
                        <input type="text" id="artist" name="artist" value="<%= song.artist %>" placeholder="Enter artist name" required>
                    </div>

                    <div class="form-row">
                        <label for="lyrics">Lyrics</label>
                        <textarea id="lyrics" name="lyrics" placeholder="Enter lyrics" required><%= song.lyrics %></textarea>
                    </div>
                    
                    <div class="form-row">
                        <label for="imageFile">Album Image</label>
                        <img src="<%= song.image %>" alt="<%= song.title %>" style="width:100px; border-radius: 4px; margin-bottom: 10px;">
                        <input type="file" id="imageFile" name="imageFile" accept="image/*">
                        <input type="hidden" name="oldImage" value="<%= song.image %>">
                    </div>

                    <div class="form-row">
                        <label for="songFile">Music File</label>
                        <audio controls>
                            <source src="<%= song.file_path %>" type="audio/mpeg">
                            Your browser does not support the audio tag.
                        </audio>
                        <input type="file" id="songFile" name="songFile" accept="audio/*">
                        <input type="hidden" name="oldFilePath" value="<%= song.file_path %>">
                    </div>
                    
                    <button type="submit" class="add-song-button">Save Changes</button>
                    <a href="/library" class="back-link">Cancel</a>
                </form>
            </div>
        </main>
        <footer>
            <p>&copy; 2024 IrohBeats</p>
        </footer>
    </div>
</body>
</html>
